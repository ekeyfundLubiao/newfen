<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="moni.anyou.com.view">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/base_theme">
        <activity android:name=".MainActivity" />
        <activity android:name=".view.IndexActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.daily.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.kindergarten.adapter.picshow.NewsPicDetaiActivity" />
        <activity android:name=".view.my.PersonInfoSettingActivity" />
        <activity android:name=".view.my.invitefamily.InviteFamilyActivity" />
        <activity android:name=".view.my.systemset.SystemSettingActivity" />
        <activity android:name=".test.CbTestActivity" />
        <activity android:name=".view.my.invitefamily.FamilyNumbersActivity" />
        <activity android:name=".view.my.systemset.UpdateLoginActivity" />
        <activity android:name=".view.account.CompleteBaseInfoActivity"></activity>
    </application>

</manifest>